# NOTE: This file is intentionally left blank.
# We make third_party/ its own bazel workspace because it allows to run `bazel build ...` without
# having all targets defined in third-party BUILD files in this directory buildable.
